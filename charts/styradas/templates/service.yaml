apiVersion: v1
kind: Service
metadata:
  name: {{ include "styradas.fullname" . }}
  labels:
    {{- include "styradas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "styradas.selectorLabels" . | nindent 4 }}
